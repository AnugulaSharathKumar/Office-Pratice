gcloud init ---> to initialize the gcloud

gcloud auth login --->Authenticate to Google Cloud

gcloud config set project <PROJECT_ID> ---->Set your GCP Project

gcloud config list ---> list the projects

gcloud compute instances list  ---->List VM Instances

gcloud compute ssh <INSTANCE_NAME> --zone <ZONE> 


